# Assign Homebrew Tap

Homebrew distribution repository for the closed-source Assign CLI binaries.
The tap contains public release assets and package metadata, not the Assign
source tree.

After the first preview release is published, install with:

```sh
brew install assignso/tap/assign
```

The package is not available until a release and `Casks/assign.rb` are present.
